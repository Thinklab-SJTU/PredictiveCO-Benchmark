dataset:
  prob_version: real

models:
  spo:
    reduction: sum
  blackbox:
    reduction: sum
  Identity:
    reduction: sum
  QPTL:
    reduction: sum
  listLTR:
    reduction: sum
    tau: 1.0
  pairLTR:
    reduction: sum
  pointLTR:
    reduction: sum
  nce:
    reduction: sum
  LODL:
    sampling: random, random_flip, random_uniform, numerical_jacobian, random_jacobian, random_hessian, random
    samplingstd: 0.1
    quadrank: 20
    quadalpha: 0
    serial: True
  dfl:
    dflalpha: 0.1
    reduction: sum

solver:
  gurobi:
    none: none